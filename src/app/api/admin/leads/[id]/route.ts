import { getAdminContext, unauthorized } from "@/lib/auth";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const context=await getAdminContext(["owner","admin","manager","sales","support"]);if(!context)return unauthorized();const{id}=await params;const input=await request.json();if(context.demo)return Response.json({ok:true,demo:true});const{error}=await context.supabase.from("leads").update({status:input.status,notes:input.notes,assigned_to:input.assignedTo}).eq("id",id);if(error)return Response.json({error:error.message},{status:400});return Response.json({ok:true});}
