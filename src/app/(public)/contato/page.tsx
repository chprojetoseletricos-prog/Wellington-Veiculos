import type { Metadata } from "next";
import { Mail, MapPin, Phone } from "lucide-react";
import { ContactForm } from "@/components/forms/contact-form";
import { getSiteSettings } from "@/lib/data";

export const metadata: Metadata = { title: "Contato", description: "Converse com a equipe Wellington Veículos." };
export default async function ContactPage() { const settings = await getSiteSettings(); return <div className="pb-24 pt-36"><div className="page-shell"><div className="grid gap-16 lg:grid-cols-[0.8fr_1.2fr]"><div><p className="eyebrow mb-4">Contato</p><h1 className="editorial-title text-5xl md:text-7xl">A conversa começa aqui.</h1><p className="mt-6 max-w-md text-sm leading-7 text-muted">Conte o que você procura. Nossa equipe responde com contexto, sem respostas automáticas que não levam a lugar algum.</p><div className="mt-10 space-y-5 border-t border-line pt-7 text-sm"><p className="flex items-start gap-3"><MapPin size={17} className="mt-0.5 text-acid" />{settings.address}<br />{settings.city}, {settings.state}</p><p className="flex items-center gap-3"><Phone size={17} className="text-acid" />{settings.phone}</p><p className="flex items-center gap-3"><Mail size={17} className="text-acid" />{settings.email}</p><p className="text-xs text-muted">{settings.hours}</p></div></div><div className="border-t border-line pt-7"><ContactForm /></div></div>{settings.mapsUrl && <iframe src={settings.mapsUrl} title="Localização da empresa" loading="lazy" referrerPolicy="no-referrer-when-downgrade" className="mt-16 h-[420px] w-full border-0 grayscale" />}</div></div>; }
